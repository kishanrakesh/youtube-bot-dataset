# ───── Python build & cache ─────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg-info/
.env/
.venv/
env/
venv/
ENV/

# ───── IDE / Editor ─────
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# ───── Logs & Outputs ─────
logs/
*.log
nohup.out
*.tmp
tmp/
*.bak
*.out

# ───── Model & data artifacts ─────
# Models directory (large trained models)
models/**/*.pkl
models/**/*.pth
models/**/*.h5
models/**/*.joblib
*.pkl
*.pth

# Data directory (large datasets)
data/raw/**
data/datasets/*.zip
data/datasets/avatar_images/**
data/processed/*.csv

# Generic data files in root (backup)
*.csv
*.npy
*.npz
*.gz
*.zip

# Keep important JSON configs but ignore data JSONs
*.json
# Allow code JSONs (configs, manifests, etc.)
!app/**/*.json
!.vscode/*.json
!config/*.json
# But still ignore secrets
config/service-account.json

# ───── Old/deprecated folders ─────
raw/
/screenshots/
logs/
env.old/

# ───── Secrets & Credentials ─────
config/service-account.json
service-account.json
.env
*.key
*.pem
*.crt
*.credentials
*.token

# ───── System files ─────
*.pid
*.lock
*.sock
*.tmp
*.cache
*.bak
*.orig
*.rej

# ───── OS / Container / Misc ─────
Thumbs.db
ehthumbs.db
desktop.ini
node_modules/
*.iml
*.db
*.sqlite
*.sqlite3

# ───── Docker / Cloud run build context ─────
# Don't ignore Dockerfile itself, but ignore local build artifacts
*.tar
*.gz
*.tgz
*.zip
build/
dist/

# ───── Local runtime/debug files ─────
tree.txt
ranked_channels_overfit.csv

# ───── GCP temporary / debug ─────
app/__pycache__/
app/utils/__pycache__/
app/pipeline/__pycache__/
app/screenshots/__pycache__/

# ───── Optional: ignore local test folders ─────
# (using tests/ not test/)
